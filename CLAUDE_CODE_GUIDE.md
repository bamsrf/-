# Гайд: Claude Code в Cursor - Pro vs API

Практическое руководство по работе с Claude Code внутри Cursor с переключением между Pro подпиской и API.

---

## 📋 Оглавление

1. [Установка Claude Code в Cursor](#установка)
2. [Настройка аутентификации](#аутентификация)
3. [Переключение между Pro и API](#переключение)
4. [Когда использовать что](#стратегия)
5. [Команды и чеклисты](#команды)
6. [Troubleshooting](#troubleshooting)

---

## 🚀 Установка Claude Code в Cursor {#установка}

### Предварительные требования

1. ✅ Установлен Cursor IDE
2. ✅ Установлен Claude Code CLI на компьютере
3. ✅ Есть Claude Pro подписка ($20/мес) или API ключ

### Установка расширения (3 способа)

#### Способ 1: Drag & Drop (Самый простой) ⭐

```bash
# 1. Найти VSIX файл Claude Code
~/.claude/local/node_modules/@anthropic-ai/claude-code/vendor/claude-code.vsix

# 2. Перетащить этот файл в окно Cursor на панель Extensions

# 3. Перезапустить Cursor
```

#### Способ 2: Командная строка

```bash
# Установить через терминал
cursor --install-extension ~/.claude/local/node_modules/@anthropic-ai/claude-code/vendor/claude-code.vsix

# Перезапустить Cursor полностью
```

#### Способ 3: Через меню Extensions

```
1. Скопировать VSIX файл в удобное место
2. Cursor → Extensions (Cmd+Shift+X)
3. Три точки "..." → "Install from VSIX..."
4. Выбрать claude-code.vsix
5. Перезапустить Cursor
```

### Проверка установки

```bash
# В Cursor открой терминал (Ctrl+`)
# Запусти Claude Code
claude

# Должна открыться панель Claude Code в Cursor
```

---

## 🔐 Настройка аутентификации {#аутентификация}

### Два режима работы

| Режим | Стоимость | Лимиты | Когда использовать |
|-------|-----------|--------|-------------------|
| **Pro подписка** | $20/мес | ~10-40 промптов/5 часов | Стандартные задачи |
| **API ключ** | Pay-as-you-go | Без лимитов | Пиковые нагрузки |

### Настройка Pro подписки

```bash
# 1. Убедись что НЕТ API ключа в environment
echo $ANTHROPIC_API_KEY
# Должно быть пустым!

# 2. Если есть - удали:
unset ANTHROPIC_API_KEY

# 3. Запусти Claude Code
claude

# 4. Выбери опцию "Login with Claude.ai"
# Откроется браузер → войди в свой Pro аккаунт

# 5. Проверь статус в Claude Code
/status
# Должно показать: "Claude Pro Account"
```

### Настройка API ключа

```bash
# 1. Получи API ключ на platform.claude.com
# Console → API Keys → Create Key

# 2. Добавь в ~/.zshrc или ~/.bashrc
export ANTHROPIC_API_KEY="sk-ant-api03-xxxxxx"

# 3. Перезагрузи конфиг
source ~/.zshrc

# 4. Проверь
echo $ANTHROPIC_API_KEY

# 5. Запусти Claude Code
claude

# 6. Проверь статус
/status
# Должно показать: "Claude API Account"
```

---

## 🔄 Переключение между Pro и API {#переключение}

### ⚠️ ВАЖНО: Приоритет аутентификации

```
Если установлен ANTHROPIC_API_KEY → используется API (даже если залогинен в Pro!)
Если НЕТ ANTHROPIC_API_KEY → используется Pro логин
```

### Сценарий 1: Из Pro → в API

**Ситуация:** Работаешь на Pro, упёрся в лимит, нужно переключиться на API

```bash
# 1. В Claude Code панели внутри Cursor
/logout

# 2. В терминале Cursor (Ctrl+`)
export ANTHROPIC_API_KEY="sk-ant-api03-xxxxxx"

# 3. Перезапусти Claude Code
# Закрой панель Claude Code и открой заново или:
claude

# 4. Проверь режим
/status
# → "Claude API Account" ✅
```

### Сценарий 2: Из API → в Pro

**Ситуация:** Работал на API, хочешь вернуться к Pro чтобы экономить

```bash
# 1. В Claude Code панели
/logout

# 2. В терминале Cursor
unset ANTHROPIC_API_KEY

# 3. Перезапусти Claude Code
claude

# 4. Логин через Pro (если спросит)
# Выбери "Login with Claude.ai" → браузер → войди

# 5. Проверь режим
/status
# → "Claude Pro Account" ✅
```

### Быстрое переключение (алиасы)

Добавь в `~/.zshrc` или `~/.bashrc`:

```bash
# === Claude Code алиасы ===

# API ключ (храни здесь, но не экспортируй всегда)
CLAUDE_API_KEY="sk-ant-api03-xxxxxx"

# Алиас для Pro режима
alias claude-pro='unset ANTHROPIC_API_KEY && echo "🔵 Режим: Claude Pro" && claude'

# Алиас для API режима
alias claude-api='export ANTHROPIC_API_KEY=$CLAUDE_API_KEY && echo "🔴 Режим: Claude API" && claude'

# Проверить текущий режим
alias claude-check='if [ -z "$ANTHROPIC_API_KEY" ]; then echo "🔵 Режим: Claude Pro"; else echo "🔴 Режим: Claude API"; fi'
```

**Использование:**

```bash
# Запустить в Pro режиме
claude-pro

# Запустить в API режиме
claude-api

# Проверить текущий режим
claude-check
```

---

## 🎯 Стратегия использования {#стратегия}

### Когда использовать Claude Pro

✅ **Стандартные задачи (используй Pro):**

- Небольшой рефакторинг (1-3 файла)
- Написание новых функций
- Исправление багов
- Code review
- Объяснение кода
- Написание тестов (небольших)
- Документация

**Характеристики:**
- 📦 Контекст: маленький-средний (< 50K токенов)
- ⏱️ Частота: ~1 промпт в 3-5 минут
- 🤖 Модель: Sonnet 4.5 или Haiku
- 💡 Сложность: низкая-средняя

### Когда переключаться на API

🔴 **Тяжелые задачи (переходи на API):**

- Большой рефакторинг (10+ файлов)
- Портирование проектов
- Генерация большой кодовой базы
- Сложная архитектура (нужен Opus)
- Batch операции (много файлов)
- Миграции фреймворков
- Полный test coverage проекта

**Характеристики:**
- 📦 Контекст: большой (100K+ токенов)
- ⏱️ Частота: много промптов подряд
- 🤖 Модель: Opus 4.5 для сложных задач
- 💡 Сложность: высокая

### Сигналы для переключения

**🔵 Pro → 🔴 API:**
```
✋ Видишь: "You've reached your usage limit"
✋ Задача требует 20+ промптов подряд
✋ Нужен Opus для архитектурных решений
✋ Работаешь с очень большим контекстом
```

**🔴 API → 🔵 Pro:**
```
💰 API баланс подходит к концу
💰 Задача стала легче/проще
💰 Можешь работать в рамках лимитов
⏰ Pro лимиты сбросились (каждые 5 часов)
```

---

## ⚡ Команды и чеклисты {#команды}

### Основные команды Claude Code

```bash
# === В терминале Cursor (Ctrl+`) ===

# Запустить Claude Code
claude

# Выйти из аккаунта (терминальная версия)
claude logout

# Войти в аккаунт
claude login

# === Внутри Claude Code панели ===

# Проверить статус аутентификации
/status

# Выйти из текущей сессии
/logout

# Войти (если нужно)
/login

# Другие полезные команды
/help          # Помощь
/clear         # Очистить историю
/plan          # Войти в режим планирования
/commit        # Создать git commit
```

### Чеклист: Начало работы

```markdown
□ Открыл Cursor
□ Открыл терминал (Ctrl+`)
□ Проверил режим: claude-check или echo $ANTHROPIC_API_KEY
□ Выбрал нужный режим (Pro или API)
□ Запустил: claude-pro или claude-api
□ Проверил статус: /status
□ Готов к работе ✅
```

### Чеклист: Переключение Pro → API

```markdown
□ Упёрся в лимит Pro
□ В Claude Code: /logout
□ В терминале: export ANTHROPIC_API_KEY="sk-ant-..."
□ Или: claude-api (если настроил алиас)
□ Перезапустил: claude
□ Проверил: /status → "Claude API Account"
□ Готов работать на API ✅
```

### Чеклист: Переключение API → Pro

```markdown
□ Хочу сэкономить, вернуться на Pro
□ В Claude Code: /logout
□ В терминале: unset ANTHROPIC_API_KEY
□ Или: claude-pro (если настроил алиас)
□ Перезапустил: claude
□ Логин через браузер (если нужно)
□ Проверил: /status → "Claude Pro Account"
□ Готов работать на Pro ✅
```

---

## 🔧 Troubleshooting {#troubleshooting}

### Проблема: Claude Code использует API вместо Pro

**Симптомы:**
- Залогинен в Pro, но `/status` показывает "API Account"
- Токены списываются с API баланса

**Решение:**
```bash
# 1. Проверь наличие API ключа
echo $ANTHROPIC_API_KEY

# 2. Если есть - удали
unset ANTHROPIC_API_KEY

# 3. Проверь ~/.zshrc и ~/.bashrc
grep ANTHROPIC_API_KEY ~/.zshrc
# Закомментируй строку если найдена

# 4. Перезагрузи конфиг
source ~/.zshrc

# 5. Перезапусти Claude Code
/logout
claude

# 6. Проверь снова
/status
```

### Проблема: "Usage limit reached" слишком быстро

**Причины:**
- Очень большой контекст проекта
- Используешь Opus вместо Sonnet
- Много файлов в контексте
- Hidden overhead (индексация)

**Решение:**
```bash
# 1. Переключись на API для этой задачи
claude-api

# 2. Или уменьши контекст:
# - Работай в меньшем скопе файлов
# - Используй .claudeignore для исключения больших папок

# 3. Подожди сброса лимита (каждые 5 часов)
```

### Проблема: Не могу установить расширение в Cursor

**Решение:**
```bash
# 1. Проверь что Claude Code установлен
which claude
# Должно показать путь

# 2. Найди VSIX файл
ls -la ~/.claude/local/node_modules/@anthropic-ai/claude-code/vendor/

# 3. Если файла нет - переустанови Claude Code CLI
npm install -g @anthropic-ai/claude-code

# 4. Попробуй другой метод установки (drag & drop)
```

### Проблема: Claude Code не открывается в Cursor

**Решение:**
```bash
# 1. Проверь что расширение установлено
# Cursor → Extensions → поиск "Claude Code"

# 2. Перезапусти Cursor полностью
# Cmd+Q → открыть заново

# 3. Попробуй через терминал внутри Cursor
# Ctrl+` → claude

# 4. Проверь логи
# Cursor → Help → Toggle Developer Tools → Console
```

---

## 📊 Мониторинг и оптимизация

### Отслеживание расходов API

```bash
# Проверить баланс API на platform.claude.com
# Console → Usage → Current billing period

# Установить лимиты:
# Console → Settings → Usage limits
# Рекомендую: $5/день для безопасности
```

### Оптимизация использования Pro

**Максимизируй использование Pro:**
- Работай в 3-часовые сессии
- Планируй сложные задачи после сброса лимита (каждые 5 часов)
- Используй Pro для research и легких задач
- Переключайся на API только когда упёрся

**Экономия API:**
- Используй Haiku для простых задач (5x дешевле Sonnet)
- Sonnet для основной работы
- Opus только для сложной архитектуры
- Prompt caching автоматически работает

---

## 🎓 Практические сценарии

### Сценарий 1: Обычный рабочий день

```bash
# 09:00 - Начало работы
claude-pro
/status  # → Pro Account

# 09:00-12:00 - Работа на Pro
# Мелкие задачи, рефакторинг, баг фиксы
# ~15-20 промптов

# 12:00 - Упёрся в лимит
"Usage limit reached"

# 12:00 - Переключение на API
/logout
claude-api
# Продолжаю работать

# 17:00 - Конец дня
# Потратил: $2-4 на API
```

### Сценарий 2: Большой рефакторинг

```bash
# Сразу начинаю на API (знаю что будет много работы)
claude-api

# Работаю 3 часа
# Рефакторинг 20+ файлов
# ~40-50 промптов
# Потратил: ~$5-7

# Задача выполнена
# Возвращаюсь на Pro для обычных задач
/logout
claude-pro
```

### Сценарий 3: Экономия бюджета

```bash
# Стратегия: максимум на Pro
claude-pro

# Работаю блоками по 3 часа с перерывами
# 09:00-12:00: работа (~20-25 промптов)
# 12:00-14:00: перерыв (лимит сбросится в 14:00!)
# 14:00-17:00: работа (~20-25 промптов)

# За день: ~40-50 промптов на Pro
# API не использовал вообще!
# Сэкономил: ~$3-5
```

---

## 💡 Лучшие практики

### ✅ DO

- Проверяй `/status` перед началом работы
- Используй алиасы для быстрого переключения
- Планируй тяжелые задачи с учетом лимитов Pro
- Отслеживай баланс API в Console
- Используй prompt caching (работает автоматически)
- Работай блоками с перерывами для сброса Pro лимитов

### ❌ DON'T

- Не держи API ключ всегда экспортированным (будет приоритет!)
- Не используй API для простых задач
- Не забывай переключаться обратно на Pro
- Не игнорируй сообщения о лимитах
- Не используй Opus через API для всего подряд

---

## 📚 Полезные ссылки

- [Claude Code Documentation](https://code.claude.com/docs)
- [Claude Pro Plan Info](https://support.claude.com/en/articles/8324991-about-claude-s-pro-plan-usage)
- [API Pricing](https://platform.claude.com/docs/en/about-claude/pricing)
- [Claude Console (API управление)](https://console.anthropic.com/)

---

## 🆘 Быстрая помощь

**Проверить текущий режим:**
```bash
/status
```

**Переключиться на Pro:**
```bash
/logout && unset ANTHROPIC_API_KEY && claude
```

**Переключиться на API:**
```bash
/logout && export ANTHROPIC_API_KEY="sk-ant-..." && claude
```

**Проверить API ключ:**
```bash
echo $ANTHROPIC_API_KEY
```

---

**Версия:** 1.0
**Дата:** 2026-01-26
**Автор:** Руководство для оптимальной работы с Claude Code в Cursor
