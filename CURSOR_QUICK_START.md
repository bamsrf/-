# Cursor AI: Быстрый старт для проекта Вертушка

## 🚀 Основы за 5 минут

### 1. Команды `/` (Slash Commands)

Введите `/` в чате Cursor, чтобы увидеть меню:

**Commands** — ваши кастомные команды (например, `/commit`)
**Actions** — встроенные действия:
- **Summarize** — суммировать код
- **Agent Review** — ревью кода агентом
- **+ Create Command** — создать новую команду

> **Примечание**: Команды вроде `/help`, `/model` и другие, описанные в некоторых руководствах, могут быть недоступны в вашей версии Cursor или работать иначе. Используйте то, что видите в меню при вводе `/`.

### 2. Символ `@` для контекста

Используйте `@` для ссылок на файлы и правила:

```
@Backend/app/models/record.py Добавь поле condition
```

```
@vinyl-records Создай функцию для поиска пластинок по артисту
```

### 3. Правила проекта

Правила находятся в `.cursor/rules/` и применяются автоматически при редактировании соответствующих файлов.

## 📝 Примеры использования

### Пример 1: Добавление нового поля в модель

```
@Backend/app/models/record.py @database
Добавь поле condition (состояние пластинки) в модель Record.
Возможные значения: mint, near_mint, very_good, good, fair, poor.
```

### Пример 2: Создание нового API endpoint

```
@api-conventions @Backend/app/api/records.py
Создай endpoint GET /records/search/by-artist для поиска пластинок по артисту.
Используй существующую схему RecordSearchResponse.
```

### Пример 3: Улучшение сканирования

```
@scanning @Mobile/app/(tabs)/index.tsx
Добавь обработку случая, когда штрихкод не распознан.
Показывай понятное сообщение пользователю.
```

### Пример 4: Работа с несколькими файлами

```
@Backend/app/models/record.py @Backend/app/schemas/record.py @Backend/app/api/records.py
Добавь поддержку поля "price" (цена покупки) во всех трёх местах:
- Модель Record
- Схема RecordResponse
- API endpoint для создания записи
```

## 🎯 Частые задачи

### Создание нового компонента

```
@mobile-conventions @Mobile/components/
Создай компонент RecordCard для отображения карточки пластинки.
Компонент должен принимать record как prop и показывать обложку, артиста, название.
```

### Исправление ошибки

```
@Backend/app/api/records.py
Исправь ошибку в функции scan_barcode: она не обрабатывает случай,
когда Discogs API возвращает пустой результат.
```

### Рефакторинг

```
@Backend/app/services/discogs.py
Вынеси логику кэширования в отдельную функцию.
Используй cachetools для кэширования результатов поиска.
```

## 💡 Советы

1. **Всегда указывайте файлы** — используйте `@filename` для точности
2. **Используйте правила** — они помогают поддерживать консистентность
3. **Комбинируйте контекст** — можно использовать несколько `@` ссылок
4. **Будьте конкретны** — чем точнее запрос, тем лучше результат

## 📚 Дополнительные ресурсы

- Полное руководство: `CURSOR_AI_GUIDE.md`
- Исследование библиотек: `ВЕРТУШКА_LIBRARIES_RESEARCH.md`
- Правила проекта: `.cursor/rules/README.md`

---

**Начните с простых задач и постепенно изучайте возможности Cursor AI!**
