/**
 * Ачивки чужого пользователя — переиспользует основной экран /achievements,
 * передавая username через query-параметр.
 */
import AchievementsScreen from '../../achievements';

export default AchievementsScreen;
