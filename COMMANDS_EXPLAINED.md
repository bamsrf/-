# Разъяснение: Какие команды нужно создавать, а какие нет

## ❌ НЕ нужно создавать кастомные команды для:

### 1. Действий, которые можно выполнить через обычные запросы

Многие действия, которые я описал как "команды", на самом деле можно выполнить просто написав запрос AI:

**Вместо команды `/Reset Context`:**
```
Просто начните новый чат или напишите: "Забудь предыдущий контекст"
```

**Вместо команды `/Add Open Files to Context`:**
```
Просто напишите: "Используй открытые файлы" или укажите @filename
```

**Вместо команды `/help`:**
```
Просто спросите: "Как использовать команду commit?" или "Что делает команда X?"
```

### 2. Действий, доступных через другие интерфейсы Cursor

Некоторые функции доступны через:
- **Настройки Cursor** (Settings) — выбор модели, настройки AI
- **Горячие клавиши** — например, Cmd+K для inline edit
- **Меню интерфейса** — различные действия через UI

### 3. Действий, которые выполняются через `@` символы

Многие задачи решаются через `@` без создания команд:

**Вместо команды `/model`:**
```
Просто выберите модель в настройках Cursor или в интерфейсе
```

**Вместо команды `/Add Active Files to Context`:**
```
Используйте @filename для явного указания файлов
```

## ✅ НУЖНО создавать кастомные команды для:

### 1. Часто повторяющихся сложных задач

Создавайте команды для действий, которые вы делаете часто и которые требуют нескольких шагов:

**Примеры полезных команд:**

**`.cursor/commands/scan-barcode.md`**
```markdown
Сканирует штрихкод пластинки, ищет в локальной БД, затем в Discogs API.
Показывает результаты и позволяет добавить в коллекцию.
```

**`.cursor/commands/add-record.md`**
```markdown
Добавляет пластинку в коллекцию по Discogs ID.
Требует discogs_id и опционально condition, price, location.
```

**`.cursor/commands/commit.md`** (уже есть)
```markdown
commit my changes to git & write short but meaningful messages
```

### 2. Специфичных для проекта задач

Команды, которые имеют смысл только для вашего проекта:

**`.cursor/commands/deploy.md`**
```markdown
Запускает скрипт деплоя из Backend/scripts/deploy.sh
Проверяет статус перед деплоем и отправляет уведомления.
```

**`.cursor/commands/test-api.md`**
```markdown
Запускает тесты для API endpoints.
Использует pytest для Backend/app/api/
```

## 🤔 Что делать с "стандартными" командами?

### Вариант 1: Не создавать их вообще

Большинство действий можно выполнить через:
- Обычные запросы к AI
- `@` символы для контекста
- Настройки и меню Cursor

### Вариант 2: Создать только те, которые реально нужны

Если вы часто выполняете какое-то действие и хотите ускорить процесс:

**Пример: Если часто нужно добавлять файлы в контекст:**
**`.cursor/commands/add-files.md`**
```markdown
Добавь все открытые файлы в контекст для работы
```

**Пример: Если часто нужно сбрасывать контекст:**
**`.cursor/commands/reset.md`**
```markdown
Сбрось контекст разговора и начни с чистого листа
```

## 📊 Сравнение подходов

| Действие | Через команду `/` | Через обычный запрос | Через `@` |
|----------|-------------------|---------------------|-----------|
| Добавить файл в контекст | `/add-files` (если создать) | "Используй открытые файлы" | `@filename` ✅ |
| Сбросить контекст | `/reset` (если создать) | "Забудь предыдущий контекст" | Новый чат ✅ |
| Справка по команде | `/help commit` (если создать) | "Что делает команда commit?" ✅ | - |
| Коммит изменений | `/commit` ✅ | "Закоммить изменения" | - |
| Сканировать пластинку | `/scan-barcode` ✅ | "Найди пластинку по штрихкоду" | - |

## 💡 Рекомендации для проекта Вертушка

### Создайте команды для:

1. **Часто используемых действий проекта:**
   - `/scan-barcode` — сканирование пластинок
   - `/add-to-collection` — добавление в коллекцию
   - `/find-stores` — поиск магазинов

2. **Автоматизации рутинных задач:**
   - `/commit` — уже есть ✅
   - `/deploy` — деплой приложения
   - `/test` — запуск тестов

### НЕ создавайте команды для:

1. **Общих действий AI:**
   - `/help` — просто спросите
   - `/reset` — начните новый чат
   - `/add-files` — используйте `@filename`

2. **Настроек Cursor:**
   - `/model` — используйте настройки
   - `/vim` — используйте настройки редактора

## 🎯 Итоговая рекомендация

**Создавайте кастомные команды только для:**
- ✅ Часто повторяющихся сложных задач
- ✅ Специфичных для проекта действий
- ✅ Автоматизации многошаговых процессов

**НЕ создавайте команды для:**
- ❌ Простых запросов, которые можно сделать обычным текстом
- ❌ Действий, доступных через `@` символы
- ❌ Настроек, доступных через UI

---

**Вывод:** Не нужно создавать все команды, которые я описал как "стандартные". Создавайте только те, которые реально ускорят вашу работу и которые вы будете использовать часто.
