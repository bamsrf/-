---
alwaysApply: true
---

# 🎯 Command Verification Rules — Проверки по типам команд

> Каждая команда агента проходит специфическую проверку перед выполнением.

---

## 📝 ТИПЫ КОМАНД И ИХ ПРОВЕРКИ

### 1️⃣ СОЗДАНИЕ ФАЙЛА (write)

**Перед созданием:**
```
[ ] Файл с таким именем не существует (или ОК перезаписать)
[ ] Путь корректен для проекта
[ ] Расширение файла соответствует содержимому
[ ] Папка существует
```

**При создании:**
```
[ ] Все импорты корректны и существуют
[ ] Экспорты соответствуют конвенциям проекта
[ ] Код соответствует стилю проекта
[ ] Нет placeholder'ов и TODO
```

**После создания:**
```
[ ] Файл интегрирован с остальным кодом
[ ] Импорты в других файлах обновлены (если нужно)
[ ] Экспорт добавлен в index файлы (если есть)
```

---

### 2️⃣ РЕДАКТИРОВАНИЕ ФАЙЛА (search_replace)

**Перед редактированием:**
```
[ ] Прочитал весь файл и понял контекст
[ ] old_string точно соответствует текущему содержимому
[ ] Понимаю, что делает редактируемый код
[ ] Изменение не сломает другие части файла
```

**При редактировании:**
```
[ ] new_string синтаксически корректен
[ ] Отступы (индентация) сохранены правильно
[ ] Импорты добавлены/обновлены если нужно
[ ] Изменение минимально и точечно
```

**После редактирования:**
```
[ ] Файл по-прежнему корректен целиком
[ ] Связанные части кода работают
[ ] Типы корректны
```

---

### 3️⃣ УДАЛЕНИЕ ФАЙЛА (delete_file)

**Перед удалением:**
```
[ ] Файл не импортируется в других местах
[ ] Нет зависимостей от этого файла
[ ] Удаление согласовано с пользователем
[ ] Есть план замены функциональности (если нужна)
```

**После удаления:**
```
[ ] Импорты в других файлах обновлены
[ ] Нет битых ссылок
[ ] Проект компилируется/работает
```

---

### 4️⃣ ВЫПОЛНЕНИЕ КОМАНДЫ (run_terminal_cmd)

**Перед выполнением:**
```
[ ] Команда безопасна (нет rm -rf /, нет деструктивных действий)
[ ] Рабочая директория корректна
[ ] Необходимые permissions запрошены
[ ] Понимаю, что команда делает
```

**Типы команд и их проверки:**

#### Git команды:
```
[ ] git add — знаю, какие файлы добавляю
[ ] git commit — сообщение понятное и информативное
[ ] git push — проверил, что локальные изменения корректны
[ ] git pull — готов к конфликтам
```

#### Package managers:
```
[ ] npm install — package существует и нужен
[ ] pip install — версия совместима
[ ] Нет уязвимых зависимостей
```

#### Docker:
```
[ ] docker compose up — конфиг актуален
[ ] docker build — Dockerfile корректен
[ ] Порты не конфликтуют
```

#### Deploy:
```
[ ] Изменения закоммичены и запушены
[ ] Миграции подготовлены
[ ] Готов к даунтайму (если будет)
```

---

### 5️⃣ ЧТЕНИЕ ФАЙЛА (read_file)

**При чтении:**
```
[ ] Путь корректен
[ ] Файл релевантен задаче
[ ] Если большой — читаю нужную часть
```

**После чтения:**
```
[ ] Понял структуру файла
[ ] Понял зависимости
[ ] Понял, что нужно изменить
```

---

### 6️⃣ ПОИСК (grep, codebase_search)

**При поиске:**
```
[ ] Поисковый запрос точный
[ ] Scope поиска адекватен (не слишком широкий)
[ ] Понимаю, что ищу
```

**После поиска:**
```
[ ] Нашёл релевантные результаты
[ ] Понял контекст найденного
[ ] Готов использовать результаты
```

---

## 🔄 СПЕЦИАЛЬНЫЕ СЦЕНАРИИ

### Добавление нового API endpoint

**Полный checklist:**
```
1. ПОДГОТОВКА:
   [ ] Прочитал существующие endpoints в том же файле
   [ ] Понял паттерн авторизации
   [ ] Понял структуру ответов
   
2. СОЗДАНИЕ:
   [ ] Добавил импорты (если нужны новые)
   [ ] Использовал правильный router
   [ ] Указал response_model
   [ ] Добавил авторизацию
   [ ] Добавил обработку ошибок
   [ ] Добавил документацию (docstring)
   
3. ПРОВЕРКА:
   [ ] Endpoint доступен по правильному пути
   [ ] HTTP метод корректный
   [ ] Типы соответствуют
   [ ] Ошибки понятные
```

### Добавление нового экрана (Mobile)

**Полный checklist:**
```
1. ПОДГОТОВКА:
   [ ] Понял структуру навигации
   [ ] Прочитал похожие экраны
   [ ] Определил нужные компоненты
   
2. СОЗДАНИЕ:
   [ ] Файл в правильной папке (app/)
   [ ] Правильное имя файла для роутинга
   [ ] Импорты корректны
   [ ] Props типизированы
   [ ] State типизирован
   
3. ИНТЕГРАЦИЯ:
   [ ] Навигация работает
   [ ] Данные загружаются
   [ ] Ошибки обрабатываются
   [ ] UI соответствует дизайну
```

### Изменение модели базы данных

**Полный checklist:**
```
1. ПОДГОТОВКА:
   [ ] Понял текущую структуру модели
   [ ] Понял связи с другими моделями
   [ ] Проверил, где модель используется
   
2. ИЗМЕНЕНИЕ:
   [ ] Поля типизированы правильно
   [ ] nullable указан явно
   [ ] Индексы добавлены где нужно
   [ ] Relationships обновлены
   
3. МИГРАЦИЯ:
   [ ] Создана миграция Alembic
   [ ] Миграция проверена (upgrade/downgrade)
   [ ] Данные не потеряются
   
4. ОБНОВЛЕНИЕ ЗАВИСИМОСТЕЙ:
   [ ] Pydantic схемы обновлены
   [ ] API endpoints обновлены
   [ ] Тесты обновлены (если есть)
```

### Деплой на production

**Полный checklist:**
```
1. ПЕРЕД ДЕПЛОЕМ:
   [ ] Все изменения закоммичены
   [ ] git push выполнен успешно
   [ ] Миграции готовы
   [ ] .env.prod актуален
   
2. ДЕПЛОЙ:
   [ ] SSH соединение работает
   [ ] Скрипт deploy.sh актуален
   [ ] Docker конфиги актуальны
   
3. ПОСЛЕ ДЕПЛОЯ:
   [ ] Контейнеры запущены (docker ps)
   [ ] Миграции применены
   [ ] API отвечает (health check)
   [ ] Нет ошибок в логах
```

---

## ⚠️ ПРЕДУПРЕЖДЕНИЯ ПО КОМАНДАМ

### Опасные команды (требуют подтверждения):
```
❌ rm -rf — НИКОГДА не использовать с /
❌ DROP TABLE — только с явного согласия
❌ git push --force — только в крайнем случае
❌ docker system prune -a — может удалить нужное
```

### Команды требующие осторожности:
```
⚠️ git reset --hard — потеря изменений
⚠️ alembic downgrade — откат миграции
⚠️ npm install с новой мажорной версией
⚠️ Изменение .env файлов
```

---

## 📋 БЫСТРЫЕ ЧЕКЛИСТЫ

### Перед любым изменением кода:
```
1. [ ] Прочитал файл
2. [ ] Понял контекст
3. [ ] План изменений ясен
4. [ ] Изменение минимально
```

### После любого изменения кода:
```
1. [ ] Код корректен
2. [ ] Типы верны
3. [ ] Ошибки обработаны
4. [ ] Стиль соблюдён
```

### Перед коммитом:
```
1. [ ] Все файлы проверены
2. [ ] Сообщение информативное
3. [ ] Конвенция соблюдена (feat/fix/etc)
```

### Перед деплоем:
```
1. [ ] Код запушен
2. [ ] Миграции готовы
3. [ ] Нет критических изменений без теста
```

---

## 🎯 ПРАВИЛО ЗОЛОТОГО СТАНДАРТА

**Перед каждым действием агент должен ответить себе на 3 вопроса:**

1. **ЧТО** я делаю?
   - Чёткое описание действия
   
2. **ПОЧЕМУ** это правильно?
   - Обоснование решения
   
3. **ЧТО МОЖЕТ ПОЙТИ НЕ ТАК?**
   - Риски и их митигация

Если на любой вопрос нет чёткого ответа — **ОСТАНОВИТЬСЯ** и уточнить.
