---
alwaysApply: true
---

# 🔄 Double-Check Rules для AI-агента — Проект Вертушка

> **Цель**: Минимизировать ошибки через обязательную двойную самопроверку перед каждым действием.
> **Применяется**: К КАЖДОЙ команде, изменению кода, и ответу.

---

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: ПЕРЕД КАЖДЫМ ДЕЙСТВИЕМ

**Перед выполнением ЛЮБОГО изменения кода агент ОБЯЗАН:**

1. **ШАГ 1 — ПОНИМАНИЕ**: Чётко сформулировать, что именно нужно сделать
2. **ШАГ 2 — АНАЛИЗ**: Проверить затрагиваемые файлы и зависимости
3. **ШАГ 3 — ПЛАН**: Составить план изменений
4. **ШАГ 4 — ПРОВЕРКА ПЛАНА**: Перепроверить план на соответствие требованиям
5. **ШАГ 5 — ВЫПОЛНЕНИЕ**: Внести изменения
6. **ШАГ 6 — ВАЛИДАЦИЯ**: Проверить результат

---

## 📋 ЧЕКЛИСТЫ ДВОЙНОЙ ПРОВЕРКИ

### ✅ Чеклист #1: Перед изменением кода

```
[ ] Прочитал и понял исходный код файла(ов)
[ ] Проверил импорты и зависимости
[ ] Проверил, что изменения не сломают другие части системы
[ ] Убедился, что стиль кода соответствует проекту
[ ] Проверил типизацию (TypeScript/Pydantic)
[ ] Проверил обработку ошибок
```

### ✅ Чеклист #2: После изменения кода

```
[ ] Код синтаксически корректен
[ ] Все импорты добавлены/обновлены
[ ] Нет неиспользуемых переменных/импортов
[ ] Типы соответствуют ожидаемым
[ ] Обработка ошибок присутствует
[ ] Нет хардкода секретов/паролей
```

### ✅ Чеклист #3: Перед деплоем

```
[ ] Изменения закоммичены с понятным сообщением
[ ] Миграции БД созданы (если нужны)
[ ] Проверена совместимость с production
[ ] Docker конфигурация актуальна
```

---

## 🔍 ПРАВИЛА ВЕРИФИКАЦИИ ПО ТИПАМ ЗАДАЧ

### 🐍 Backend (FastAPI/Python)

**Перед изменением:**
```
1. ПРОЧИТАТЬ: app/main.py — понять структуру приложения
2. ПРОЧИТАТЬ: Затрагиваемый файл в app/api/
3. ПРОВЕРИТЬ: app/models/ — модели данных
4. ПРОВЕРИТЬ: app/schemas/ — Pydantic схемы
5. ПРОВЕРИТЬ: Зависимости в requirements.txt
```

**Двойная проверка кода:**
```
1. Endpoint использует правильный HTTP метод?
2. Авторизация через Depends(get_current_user) добавлена?
3. Pydantic схема для request/response указана?
4. HTTPException с понятным сообщением для ошибок?
5. Логирование ошибок присутствует?
6. Транзакции БД корректно обработаны?
```

**Пример правильного endpoint:**
```python
@router.get("/items/{item_id}", response_model=ItemResponse)
async def get_item(
    item_id: UUID = Path(..., description="ID элемента"),
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """Получение элемента по ID"""
    try:
        result = await db.execute(
            select(Item).where(Item.id == item_id)
        )
        item = result.scalar_one_or_none()
        
        if not item:
            raise HTTPException(status_code=404, detail="Элемент не найден")
        
        return ItemResponse.from_orm(item)
    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"Error: {e}", exc_info=True)
        raise HTTPException(status_code=500, detail="Внутренняя ошибка")
```

### 📱 Mobile (React Native/Expo)

**Перед изменением:**
```
1. ПРОЧИТАТЬ: app/_layout.tsx — понять навигацию
2. ПРОЧИТАТЬ: Затрагиваемый экран в app/
3. ПРОВЕРИТЬ: lib/api.ts — API endpoints
4. ПРОВЕРИТЬ: lib/store.ts — Zustand stores
5. ПРОВЕРИТЬ: lib/types.ts — TypeScript типы
6. ПРОВЕРИТЬ: components/ — используемые компоненты
```

**Двойная проверка кода:**
```
1. Типы для props и state определены?
2. Loading состояние обрабатывается?
3. Ошибки перехватываются и показываются пользователю?
4. useEffect очищает подписки в cleanup?
5. Стили через StyleSheet.create?
6. Компонент оптимизирован (useMemo/useCallback где нужно)?
```

**Пример правильного компонента:**
```typescript
import { useState, useCallback } from 'react';
import { Alert, ActivityIndicator } from 'react-native';
import { useStore } from '@/lib/store';

interface Props {
  itemId: string;
  onSuccess?: () => void;
}

export function ItemComponent({ itemId, onSuccess }: Props) {
  const [isLoading, setIsLoading] = useState(false);
  const { fetchItem } = useStore();

  const handleFetch = useCallback(async () => {
    if (isLoading) return;
    
    setIsLoading(true);
    try {
      await fetchItem(itemId);
      onSuccess?.();
    } catch (error) {
      Alert.alert('Ошибка', 'Не удалось загрузить данные');
    } finally {
      setIsLoading(false);
    }
  }, [itemId, isLoading, fetchItem, onSuccess]);

  if (isLoading) return <ActivityIndicator />;
  
  // ...render
}
```

### 🗃️ База данных (SQLAlchemy/Alembic)

**Перед изменением модели:**
```
1. ПРОЧИТАТЬ: Существующую модель в app/models/
2. ПРОВЕРИТЬ: Связи с другими моделями
3. ПРОВЕРИТЬ: Индексы и constraints
4. ПОДГОТОВИТЬ: План миграции
```

**Двойная проверка:**
```
1. UUID для primary key?
2. Mapped типы SQLAlchemy 2.0?
3. Nullable явно указан?
4. Индексы для поиска добавлены?
5. Relationship с back_populates?
6. created_at/updated_at поля есть?
```

**После изменения модели:**
```bash
# ОБЯЗАТЕЛЬНО создать миграцию:
cd Вертушка/Backend
alembic revision --autogenerate -m "описание изменений"
alembic upgrade head  # применить локально для проверки
```

---

## 🛡️ ПРАВИЛА БЕЗОПАСНОСТИ

### Запрещено:
- ❌ Хардкодить секреты, токены, пароли
- ❌ Отключать авторизацию без явной необходимости
- ❌ Логировать sensitive данные (пароли, токены)
- ❌ Возвращать детали внутренних ошибок клиенту
- ❌ Использовать `eval()` или `exec()`
- ❌ Игнорировать валидацию входных данных

### Обязательно:
- ✅ Использовать переменные окружения для секретов
- ✅ Валидировать все входные данные (Pydantic, TypeScript)
- ✅ Использовать параметризованные запросы к БД
- ✅ Обрабатывать все исключения
- ✅ Логировать подозрительную активность

---

## 🔄 ПРОЦЕСС ДВОЙНОЙ ПРОВЕРКИ ДЛЯ АГЕНТА

### Перед ЛЮБЫМ изменением файла:

**Шаг 1: Анализ (ОБЯЗАТЕЛЬНО выполнить)**
```
Агент должен:
1. Прочитать файл полностью (read_file)
2. Понять текущую структуру и логику
3. Найти все зависимости и импорты
4. Определить scope изменений
```

**Шаг 2: Планирование (ОБЯЗАТЕЛЬНО сформулировать)**
```
Агент должен:
1. Описать, что будет изменено
2. Описать, почему именно так
3. Перечислить файлы, которые будут затронуты
4. Предупредить о возможных рисках
```

**Шаг 3: Исполнение (С проверкой)**
```
Агент должен:
1. Вносить изменения минимально-необходимые
2. Не ломать существующую функциональность
3. Следовать стилю кодирования проекта
4. Добавлять обработку ошибок
```

**Шаг 4: Валидация (ОБЯЗАТЕЛЬНО выполнить)**
```
Агент должен:
1. Перечитать изменённый файл
2. Проверить синтаксис и типы
3. Убедиться, что все импорты корректны
4. Проверить обработку edge cases
```

---

## 📝 ФОРМАТ ОТВЕТА АГЕНТА

При внесении изменений агент должен следовать формату:

```markdown
## 🔍 Анализ задачи
[Что нужно сделать и почему]

## 📁 Затрагиваемые файлы
- file1.py — [что меняем]
- file2.tsx — [что меняем]

## ⚠️ Потенциальные риски
- [Риск 1]
- [Риск 2]

## ✅ План изменений
1. [Шаг 1]
2. [Шаг 2]

## 🔄 Двойная проверка
- [ ] Код читаем и понятен
- [ ] Типизация корректна
- [ ] Ошибки обработаны
- [ ] Стиль соответствует проекту

[Изменения кода]

## ✔️ Валидация
[Подтверждение, что изменения корректны]

Don't forget to commit! 
`git add . && git commit -m "тип: описание изменений"`
```

---

## 🚫 АНТИПАТТЕРНЫ (Что НЕЛЬЗЯ делать)

### Код:
```python
# ❌ ПЛОХО: Нет обработки ошибок
@router.get("/items/{id}")
async def get_item(id: str):
    return await db.get(Item, id)

# ✅ ХОРОШО: Полная обработка
@router.get("/items/{id}", response_model=ItemResponse)
async def get_item(
    id: UUID = Path(...),
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    try:
        item = await db.get(Item, id)
        if not item:
            raise HTTPException(404, "Не найдено")
        return ItemResponse.from_orm(item)
    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"Error: {e}")
        raise HTTPException(500, "Ошибка сервера")
```

```typescript
// ❌ ПЛОХО: Нет типов и обработки ошибок
const fetchData = async (id) => {
  const data = await api.get(`/items/${id}`);
  setData(data);
};

// ✅ ХОРОШО: Типизация и обработка ошибок
const fetchData = useCallback(async (id: string): Promise<void> => {
  setIsLoading(true);
  try {
    const { data } = await api.get<Item>(`/items/${id}`);
    setData(data);
  } catch (error) {
    Alert.alert('Ошибка', 'Не удалось загрузить данные');
  } finally {
    setIsLoading(false);
  }
}, []);
```

---

## 🏁 ФИНАЛЬНЫЙ ЧЕКЛИСТ ПЕРЕД ЗАВЕРШЕНИЕМ

Агент должен убедиться:

```
[ ] Все изменения соответствуют запросу пользователя
[ ] Код синтаксически корректен
[ ] Типизация полная и корректная
[ ] Обработка ошибок присутствует везде
[ ] Нет хардкода секретов
[ ] Стиль соответствует проекту
[ ] Импорты корректны и полны
[ ] Предложена команда для коммита
```

---

## 📚 Ссылки на файлы проекта

### Backend:
- @Вертушка/Backend/app/main.py
- @Вертушка/Backend/app/api/
- @Вертушка/Backend/app/models/
- @Вертушка/Backend/app/schemas/

### Mobile:
- @Вертушка/Mobile/app/
- @Вертушка/Mobile/lib/api.ts
- @Вертушка/Mobile/lib/store.ts
- @Вертушка/Mobile/lib/types.ts
- @Вертушка/Mobile/components/

### Деплой:
- @Вертушка/Backend/scripts/deploy.sh
- @Вертушка/Backend/docker-compose.prod.yml
