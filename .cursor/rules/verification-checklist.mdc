---
alwaysApply: true
---

# ✓ Verification Checklist — Детальные проверки для Вертушка

> Этот документ содержит конкретные проверки, которые агент ОБЯЗАН выполнять.

---

## 🔴 КРИТИЧЕСКИЕ ПРОВЕРКИ (БЛОКИРУЮЩИЕ)

Если хоть одна не пройдена — **ОСТАНОВИТЬ** и исправить.

### 1. Безопасность данных
```
[ ] НЕТ хардкода паролей, токенов, API ключей
[ ] НЕТ вывода sensitive данных в логи
[ ] Секреты берутся из environment variables
[ ] SQL запросы параметризованы (нет SQL injection)
```

### 2. Синтаксис и типы
```
[ ] Код парсится без ошибок
[ ] Все импорты существуют и корректны
[ ] TypeScript типы определены для всех параметров
[ ] Pydantic схемы валидны
```

### 3. Целостность системы
```
[ ] Изменения не ломают существующий API контракт
[ ] Backward compatibility сохранена
[ ] Миграции БД созданы при изменении моделей
```

---

## 🟡 ВАЖНЫЕ ПРОВЕРКИ (РЕКОМЕНДУЕМЫЕ)

### Backend (Python/FastAPI)

#### При создании/изменении endpoint:
```
[ ] response_model указан
[ ] HTTP метод соответствует действию (GET/POST/PUT/DELETE)
[ ] Path параметры типизированы (UUID, int, str)
[ ] Query параметры имеют описания
[ ] Авторизация через Depends(get_current_user)
[ ] HTTPException для всех ошибок с понятными сообщениями
[ ] try/except для внешних вызовов (БД, API)
[ ] Логирование ошибок с exc_info=True
```

#### При изменении модели SQLAlchemy:
```
[ ] UUID для primary_key
[ ] Mapped типы (SQLAlchemy 2.0 стиль)
[ ] nullable явно указан
[ ] Индексы для полей поиска
[ ] relationship с back_populates
[ ] cascade правильно настроен
[ ] created_at/updated_at если нужны
```

#### При создании Pydantic схемы:
```
[ ] Все поля типизированы
[ ] Optional для nullable полей
[ ] Field с description для документации
[ ] Validators где нужна кастомная валидация
[ ] from_orm = True в Config если ORM модель
```

### Mobile (TypeScript/React Native)

#### При создании/изменении компонента:
```
[ ] Props interface определён
[ ] Типы для state определены
[ ] useCallback для функций переданных в children
[ ] useMemo для тяжёлых вычислений
[ ] Loading state обрабатывается
[ ] Error state обрабатывается
[ ] Alert/Toast для ошибок пользователю
[ ] Cleanup в useEffect
```

#### При работе с API:
```
[ ] Типы для request/response
[ ] try/catch для API вызовов
[ ] Loading индикатор во время запроса
[ ] Понятное сообщение при ошибке
[ ] Повторный запрос возможен (retry)
[ ] Debounce для частых запросов
```

#### При работе с Zustand store:
```
[ ] Типизация state и actions
[ ] Селекторы для подписки на часть state
[ ] Async actions обрабатывают ошибки
[ ] Reset action если нужен
```

---

## 🔵 СТИЛЕВЫЕ ПРОВЕРКИ

### Python
```
[ ] snake_case для функций и переменных
[ ] PascalCase для классов
[ ] Docstrings для публичных функций
[ ] Максимум 88 символов в строке (black)
[ ] Импорты отсортированы (isort)
[ ] Нет unused imports
```

### TypeScript/React Native
```
[ ] camelCase для переменных и функций
[ ] PascalCase для компонентов и интерфейсов
[ ] kebab-case для файлов и папок
[ ] Именованный экспорт для компонентов
[ ] StyleSheet.create для стилей
[ ] Нет inline стилей (кроме динамических)
```

---

## 📊 МАТРИЦА ПРОВЕРОК ПО ТИПАМ ЗАДАЧ

| Задача | Критические | Важные | Стилевые |
|--------|-------------|--------|----------|
| Новый endpoint | ✅ Все | ✅ Все | ✅ Python |
| Изменение endpoint | ✅ Все | ✅ Endpoint | ✅ Python |
| Новая модель | ✅ Все | ✅ Модель | ✅ Python |
| Новый компонент | ✅ Все | ✅ Компонент | ✅ TypeScript |
| Изменение компонента | ✅ Все | ✅ Компонент | ✅ TypeScript |
| Работа с API (mobile) | ✅ Все | ✅ API | ✅ TypeScript |
| Миграция БД | ✅ Все | ✅ Модель | — |
| Конфигурация | ✅ Безопасность | — | — |

---

## 🔁 WORKFLOW ДВОЙНОЙ ПРОВЕРКИ

### Этап 1: Pre-Check (До изменений)
```
1. Прочитать исходные файлы
2. Понять текущую архитектуру
3. Определить scope изменений
4. Проверить зависимости
```

### Этап 2: Implementation (Изменения)
```
1. Внести минимально необходимые изменения
2. Следовать существующему стилю
3. Добавить обработку ошибок
4. Добавить типы
```

### Этап 3: Post-Check (После изменений)
```
1. Перечитать изменённый код
2. Проверить по чеклисту выше
3. Убедиться в полноте
4. Проверить edge cases
```

### Этап 4: Validation (Валидация)
```
1. Убедиться, что код работает
2. Проверить на типичные ошибки
3. Предложить тест (если applicable)
4. Предложить commit message
```

---

## 🚨 ТИПИЧНЫЕ ОШИБКИ (Что проверить дважды)

### Backend:
```
❌ Забыл await для async функции
❌ Не обработал None при .first()/.one_or_none()
❌ HTTPException внутри except без re-raise
❌ Логирование без exc_info
❌ Отсутствует response_model
```

### Mobile:
```
❌ Бесконечный useEffect (нет deps)
❌ Мутация state напрямую
❌ Отсутствует cleanup в useEffect
❌ Забыл await в async функции
❌ Нет обработки loading/error state
```

### Database:
```
❌ Забыл создать миграцию
❌ Миграция не применена
❌ Foreign key без cascade
❌ Нет индекса для частого поиска
```

---

## ✅ ШАБЛОН ПРОВЕРКИ ДЛЯ АГЕНТА

При каждом изменении агент должен мысленно пройти:

```markdown
## 🔍 Self-Check Report

### Критические проверки:
- [x] Безопасность: нет хардкода секретов
- [x] Синтаксис: код парсится
- [x] Импорты: все существуют
- [x] Типы: все определены

### Проверки по типу задачи:
- [x] [Специфические для задачи]

### Стиль:
- [x] Соответствует проекту

### Финальная валидация:
- [x] Изменения минимальны и точны
- [x] Не сломано существующее
- [x] Edge cases учтены
```

---

## 📌 QUICK REFERENCE

### Быстрая проверка Backend endpoint:
```
1. response_model? ✓
2. Depends(get_current_user)? ✓
3. try/except? ✓
4. HTTPException для ошибок? ✓
5. logger.error? ✓
```

### Быстрая проверка React компонента:
```
1. Props interface? ✓
2. Loading state? ✓
3. Error handling? ✓
4. useCallback/useMemo? ✓
5. Cleanup? ✓
```

### Быстрая проверка модели:
```
1. UUID pk? ✓
2. Mapped types? ✓
3. nullable? ✓
4. indexes? ✓
5. relationships? ✓
```
